# Subti 0.3.4

**Licence keys issued by subti.app are accepted by this build.**

## Fixed

- A licence key bought from subti.app now verifies. This build carries the signing key those keys
  are made with; earlier ones checked against an older key and refused every one of them.

---

**Version** 0.3.4 · **Build** 7 · **Requires** macOS 14.0 or later

### SHA-256

```
6a4303122d4e8a9b381d2b875776dda2ed91a3a20edb6e4adade287b98201a82  Subti-0.3.4.dmg
```

```bash
shasum -a 256 ~/Downloads/Subti-0.3.4.dmg
```

The download at [https://subti.app](https://subti.app) is the canonical one, and the app updates itself from there. This
release is a mirror, published after the release went out, so the checksums above can be read from
somewhere other than the site that served the file.

